version: "3.8"
name: lookout

services:
  lookout_core:
    build:
      args:
        GPU: ${GPU:-true}
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
