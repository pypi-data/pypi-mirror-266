# Test of bleeding data back into the stack because of objects being referenced and not literal values only.


var_a = 1;
var_b = 2;
var_c = var_a / var_b;

print(var_a);
print(var_b);
print(var_c);

q = 1;
print(q); #? don't know but it seems no problem?