# This function does go a step further, in that we attempt to assign the return value of the function to a variable.

func => (integer a) integer {
    return a + 1;
}

var = func(1);
print(var);

var2 = func(var);
print(var2);
