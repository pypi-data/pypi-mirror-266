# This is a basic fibonaci function to test the compiler.
# We still work around the problem that we can not pass functions into function arguments.

fib => (n, t, o, q) {
    #print(n);
    #if n == 1 or n == 2 {
    #    #print(n);
    #}
    x = [1, 2, 3];
    #foreach x {
    #    #print(it);
    #}
    #return fib(n-1) + fib(n-2);
    #print("returning");
    return n;
}

var = fib(2, 4, 9, 2);
print("fib(7) = ", var);

v2 = fib(6, 2, 3, 4);
print(v2);

