from setuptools import setup, find_packages

with open("README.md", "r") as f:
    description = f.read()

setup(
    name="Terry-CRPrint",
    version="0.1",
    packages=find_packages(),
    install_requires=[]
)