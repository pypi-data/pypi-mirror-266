[tool.poetry]
name = "marca"
version = "0.1.0"
description = "Modular Association Rules for Classification Algorithms"
authors = ["Maicondalllg <maicon.dallagnol@unesp.br>"]
readme = "README.md"

packages = [{include = "marca"}]

[tool.poetry.dependencies]
python = "^3.8"
pandas = "^2.2.0"
numpy = "^1.21.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"