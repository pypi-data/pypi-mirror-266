from setuptools import setup, find_packages

setup(
  name='subprocess_loop',
  version='1.0.9',
  author='dev mx',
  description='subprocess exchange with stdio',
  url='https://github.com/maxavaiable/py-subprocess-loop',
  packages=find_packages(),
  python_requires='>=3.0'
)