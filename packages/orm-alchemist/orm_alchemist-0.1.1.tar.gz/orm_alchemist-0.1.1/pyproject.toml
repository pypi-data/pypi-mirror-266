[tool.poetry]
name = "orm-alchemist"
version = "0.1.1"
description = "SQLAlchemy boilerplate for FastAPI"
authors = ["izagood <ljbfif50@gmail.com>"]
readme = "README.md"
license = "MIT"
repository = "https://github.com/izagood/orm-alchemist"
packages = [{include = "orm_alchemist"}]

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
