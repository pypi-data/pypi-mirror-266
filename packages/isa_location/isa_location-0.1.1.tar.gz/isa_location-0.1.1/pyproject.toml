[tool.poetry]
authors = ["sasya"]
description = "well, this is my first poetry project"
name = "isa_location"
readme = "README.md"
version = "0.1.1"

[tool.poetry.dependencies]
elasticsearch = "^8.13.0"
pip = "^24.0"
python = "^3.8.2"
requests = "^2.31.0"

# [tool.poetry.repositories]
# my-repository = {url = "http://localhost:8080/"}

[build-system]
build-backend = "poetry.core.masonry.api"
requires = ["poetry-core"]
