[tool.poetry]
name = "normalize-companies-names"
version = "1.0.1"
description = ""
authors = ["WillersonAbreu <willersonabreu@hotmail.com>"]
readme = "README.md"
packages = [
    { include = "src", format = ["sdist",  "wheel"] }
]

[tool.poetry.dependencies]
python = "^3.10"
thefuzz = "^0.22.1"
pandas = "^2.2.1"
openpyxl = "^3.1.2"
wheel = "^0.43.0"
click = "^8.1.7"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
normalize = "src.main:main"