[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
version = "0.0.1"
name = "tdmd"
description = "CLI Todo Application, that converts all your records into pretty markdown"
authors = [{ name="Alex Zolotarov", email="alex@zolotarov.me" }]
readme = "README.md"
dependencies = ["fire"]
requires-python = ">=3.9"
