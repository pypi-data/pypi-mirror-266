[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
version = "0.0.2"
name = "tdmd"
description = "Manage your tasks using the command line interface and view your plans in a visually appealing markdown file."
authors = [{ name="Alex Zolotarov", email="alex@zolotarov.me" }]
readme = "README.md"
dependencies = ["fire"]
requires-python = ">=3.9"
