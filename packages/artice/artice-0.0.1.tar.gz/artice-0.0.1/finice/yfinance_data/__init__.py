"""yfinance is an open-source financial data library, whose backend is Yahoo Finance api.

`yfinance_data` support unified data retrive and access api for yfinance data.

The data retrive support `download_history` and `download_latest` to support both historical and latest delta update.

The data output directory has a layout as follows:
- `
"""
from .reader import *
