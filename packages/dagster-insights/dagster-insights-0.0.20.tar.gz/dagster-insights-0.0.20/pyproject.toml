[project]
name = "dagster-insights"
version = "0.0.20"
description = "Latest pre-release patches for dagster_cloud.dagster_insights."
readme = "README.md"
dependencies = [
    "dagster-cloud",
]

authors = [
  { name="Dagster Labs" },
]

[project.license]
file = "LICENSE.md"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
