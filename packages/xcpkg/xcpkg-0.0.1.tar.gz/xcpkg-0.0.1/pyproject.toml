[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "xcpkg"
version = "0.0.1"
requires-python = ">=3.12"
authors = [
  {name = "YeyingXingchen", email = "yeyingxingchen@yeah.net"}
]
description = "星辰包管理器。"
readme = "README.md"
license = {file = "LICENSE"}
keywords = ["xcpkg", "package", "manager"]

[project.scripts]
xcpkg = "xcpkg:main"