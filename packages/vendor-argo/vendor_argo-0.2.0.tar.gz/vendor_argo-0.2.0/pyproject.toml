[project]
name = "vendor-argo"
version = "0.2.0"
description = "Add your description here"
authors = [
    { name = "yokotoka", email = "hey@yokotoka.is" }
]
dependencies = [
    "django>=5.0.4",
    "djangorestframework>=3.15.1",
]
readme = "README.md"
requires-python = ">= 3.8"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = []

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/vendor_argo"]
