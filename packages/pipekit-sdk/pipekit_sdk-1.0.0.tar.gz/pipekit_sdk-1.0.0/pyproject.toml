[build-system]
requires = ["hatchling", "hera >= 5.5.2"]
build-backend = "hatchling.build"

[project]
name = "pipekit-sdk"
version = "1.0.0"
authors = [
  { name="Pipekit", email="ci@pipekit.io" },
]
description = "Pipekit Python SDK"
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]

dependencies = [
  "hera >= 5.5.2",
]
