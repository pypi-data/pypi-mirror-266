


[tool.poetry]
name = "poetry-kit"
version = "0.1.5"
description = ""
authors = []
readme = "readme.md"
packages = [
    { include = "poetry_kit", from = "venues/stages" },
]

[tool.poetry.dependencies]
python = "^3.10"
volts = "^1.0.9"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

