[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "celitech-test-sdk"
version = "1.0.0"
license = { text = "MIT" }
description = """Welcome to the CELITECH API documentation!  Useful links: [Homepage](https://www.celitech.com)"""
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "requests>=2.31.0"
]
